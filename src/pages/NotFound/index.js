import React from 'react';
import Title from '../../components/Title';
import { CenteredTitle } from './styles';

const NotFound = () => (
  <CenteredTitle>
    <Title
      header="No thighs anywhere in sight..."
      subheader="I know, it's a shame - maybe the URL is wrong?"
    />
  </CenteredTitle>
);

export default NotFound;
